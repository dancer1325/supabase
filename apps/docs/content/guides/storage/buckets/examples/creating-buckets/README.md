# prerequisites
* download Docker Desktop
* `npx supabase init` OR `supabase init`
* `npx supabase start` OR `supabase start`

# ways to create a bucket
## -- via -- Supabase Dashboard
* follow the steps
## -- via -- SQL
* run [sample.sql](sample.sql)
## -- via -- Supabase client libraries
TODO:

# how to restrict uploads?
## by type -- `allowedMimeTypes` --
* follows steps to create via Supabase Dashboard > enable > specify it
## by files' size -- `fileSizeLimit` --
* follows steps to create via Supabase Dashboard > enable > specify it
